# -*- generated by 1.0.9 -*-
import da
PatternExpr_194 = da.pat.TuplePattern([da.pat.ConstantPattern('req_operation'), da.pat.FreePattern('operation'), da.pat.FreePattern('client'), da.pat.FreePattern('client_index')])
PatternExpr_224 = da.pat.TuplePattern([da.pat.ConstantPattern('forward_shuttle'), da.pat.FreePattern('replica'), da.pat.FreePattern('forward_shuttle'), da.pat.FreePattern('client')])
PatternExpr_431 = da.pat.TuplePattern([da.pat.ConstantPattern('req_operation'), da.pat.FreePattern('operation'), da.pat.FreePattern('client'), da.pat.FreePattern('client_index')])
PatternExpr_601 = da.pat.TuplePattern([da.pat.ConstantPattern('result_shuttle'), da.pat.FreePattern('replica'), da.pat.FreePattern('result_shuttle')])
PatternExpr_627 = da.pat.TuplePattern([da.pat.ConstantPattern('forward_shuttle'), da.pat.FreePattern('replica'), da.pat.FreePattern('forward_shuttle'), da.pat.FreePattern('client')])
PatternExpr_774 = da.pat.TuplePattern([da.pat.ConstantPattern('result_shuttle'), da.pat.FreePattern('replica'), da.pat.FreePattern('result_shuttle')])
PatternExpr_920 = da.pat.TuplePattern([da.pat.ConstantPattern('forward_shuttle'), da.pat.FreePattern('replica'), da.pat.FreePattern('forward_shuttle'), da.pat.FreePattern('client')])
PatternExpr_946 = da.pat.TuplePattern([da.pat.ConstantPattern('result_shuttle'), da.pat.FreePattern('replica'), da.pat.FreePattern('result_shuttle')])
PatternExpr_986 = da.pat.TuplePattern([da.pat.ConstantPattern('req_operation'), da.pat.FreePattern('operation'), da.pat.FreePattern('client'), da.pat.FreePattern('client_index')])
PatternExpr_1027 = da.pat.TuplePattern([da.pat.ConstantPattern('forward_shuttle'), da.pat.FreePattern('replica'), da.pat.FreePattern('forward_shuttle'), da.pat.FreePattern('client')])
_config_object = {}

class Replica(da.DistProcess):

    def __init__(self, procimpl, props):
        super().__init__(procimpl, props)
        self._ReplicaReceivedEvent_0 = []
        self._ReplicaReceivedEvent_1 = []
        self._ReplicaReceivedEvent_3 = []
        self._ReplicaReceivedEvent_5 = []
        self._ReplicaReceivedEvent_6 = []
        self._ReplicaReceivedEvent_8 = []
        self._ReplicaReceivedEvent_9 = []
        self._events.extend([da.pat.EventPattern(da.pat.ReceivedEvent, '_ReplicaReceivedEvent_0', PatternExpr_194, sources=None, destinations=None, timestamps=None, record_history=True, handlers=[]), da.pat.EventPattern(da.pat.ReceivedEvent, '_ReplicaReceivedEvent_1', PatternExpr_224, sources=None, destinations=None, timestamps=None, record_history=True, handlers=[]), da.pat.EventPattern(da.pat.ReceivedEvent, '_ReplicaReceivedEvent_2', PatternExpr_431, sources=None, destinations=None, timestamps=None, record_history=None, handlers=[self._Replica_handler_430]), da.pat.EventPattern(da.pat.ReceivedEvent, '_ReplicaReceivedEvent_3', PatternExpr_601, sources=None, destinations=None, timestamps=None, record_history=True, handlers=[]), da.pat.EventPattern(da.pat.ReceivedEvent, '_ReplicaReceivedEvent_4', PatternExpr_627, sources=None, destinations=None, timestamps=None, record_history=None, handlers=[self._Replica_handler_626]), da.pat.EventPattern(da.pat.ReceivedEvent, '_ReplicaReceivedEvent_5', PatternExpr_774, sources=None, destinations=None, timestamps=None, record_history=True, handlers=[]), da.pat.EventPattern(da.pat.ReceivedEvent, '_ReplicaReceivedEvent_6', PatternExpr_920, sources=None, destinations=None, timestamps=None, record_history=True, handlers=[]), da.pat.EventPattern(da.pat.ReceivedEvent, '_ReplicaReceivedEvent_7', PatternExpr_946, sources=None, destinations=None, timestamps=None, record_history=None, handlers=[self._Replica_handler_945]), da.pat.EventPattern(da.pat.ReceivedEvent, '_ReplicaReceivedEvent_8', PatternExpr_986, sources=None, destinations=None, timestamps=None, record_history=True, handlers=[]), da.pat.EventPattern(da.pat.ReceivedEvent, '_ReplicaReceivedEvent_9', PatternExpr_1027, sources=None, destinations=None, timestamps=None, record_history=True, handlers=[])])

    def setup(self, olympus, replicaList, index, type, database, history, **rest_1051):
        super().setup(olympus=olympus, replicaList=replicaList, index=index, type=type, database=database, history=history, **rest_1051)
        self._state.olympus = olympus
        self._state.replicaList = replicaList
        self._state.index = index
        self._state.type = type
        self._state.database = database
        self._state.history = history
        self.output('Replica', self._state.index, ':>> Setting up Replica: ', self._state.type)

    def run(self):
        self.output('Replica', self._state.index, ':>> Replica Started: ', self._state.type)
        if (self._state.type == 'head'):
            super()._label('_st_label_191', block=False)
            client_index = client = operation = None

            def ExistentialOpExpr_192():
                nonlocal client_index, client, operation
                for (_, _, (_ConstantPattern213_, operation, client, client_index)) in self._ReplicaReceivedEvent_0:
                    if (_ConstantPattern213_ == 'req_operation'):
                        if True:
                            return True
                return False
            _st_label_191 = 0
            while (_st_label_191 == 0):
                _st_label_191 += 1
                if ExistentialOpExpr_192():
                    _st_label_191 += 1
                else:
                    super()._label('_st_label_191', block=True)
                    _st_label_191 -= 1
        else:
            super()._label('_st_label_221', block=False)
            forward_shuttle = replica = client = None

            def ExistentialOpExpr_222():
                nonlocal forward_shuttle, replica, client
                for (_, _, (_ConstantPattern242_, replica, forward_shuttle, client)) in self._ReplicaReceivedEvent_1:
                    if (_ConstantPattern242_ == 'forward_shuttle'):
                        if True:
                            return True
                return False
            _st_label_221 = 0
            while (_st_label_221 == 0):
                _st_label_221 += 1
                if ExistentialOpExpr_222():
                    _st_label_221 += 1
                else:
                    super()._label('_st_label_221', block=True)
                    _st_label_221 -= 1

    def performOperation(self, operation):
        self.output('Replica', self._state.index, ':>> perform operation: ', operation.command)
        import re
        matches = re.findall("\\'(.+?)\\'", operation.command)
        key = None
        value = None
        if (not (operation.type is 2)):
            key = matches[0]
            value = matches[1]
        else:
            key = matches[0]
        self.output('Replica', self._state.index, ':>> Received value from Client', key, value)
        if (operation.type is 1):
            self._state.database[key] = value
            self.output('Replica', self._state.index, ':>> After performing operation: ', self._state.database)
            return 'ok'
        elif (operation.type is 2):
            if (key in self._state.database):
                self.output('Replica', self._state.index, ':>> After performing operation: ', self._state.database)
                return self._state.database[key]
            else:
                self.output('Replica', self._state.index, ':>> After performing operation: ', self._state.database)
                return ''
        elif (operation.type is 3):
            self.output('Replica', self._state.index, ':>> After performing operation: ', self._state.database)
            return 'ok'
        elif (operation.type is 4):
            if (key in self._state.database):
                val = self._state.database[key]
                list_val = (val + value)
                self._state.database[key] = list_val
            else:
                return 'fail'
            self.output('Replica', self._state.index, ':>> After performing operation: ', self._state.database)

    def _Replica_handler_430(self, operation, client, client_index):
        self.output('Replica', self._state.index, ':>> Received req_operation from Client', client_index)
        result = self.performOperation(operation)
        if (self._state.type == 'head'):
            self.output('Replica', self._state.index, ':>> Operation received at head node')
            self.output('Replica', self._state.index, ':>> Head Node will assign a sequence id')
            class_action = da.import_da('Action')
            action = class_action.Action()
            action.seq_id = 1
            action.operation = operation
            order_proof_list = []
            result_proof_list = []
            class_order_proof = da.import_da('OrderProof')
            order_proof = class_order_proof.OrderProof()
            order_proof.order = 'order'
            order_proof.action = action
            order_proof_list.append(order_proof)
            class_result_proof = da.import_da('ResultProof')
            result_proof = class_result_proof.ResultProof()
            result_proof.result_const = 'result'
            result_proof.action = action
            result_proof.result = result
            result_proof_list.append(result_proof)
            class_shuttle = da.import_da('Shuttle')
            forward_shuttle = class_shuttle.Shuttle()
            forward_shuttle.action = action
            forward_shuttle.order_proof_list = order_proof_list
            forward_shuttle.result_proof_list = result_proof_list
            self.output('Replica', self._state.index, ':>> Forwarding shuttle to Replica', (self._state.index + 1))
            self.send(('forward_shuttle', self._id, forward_shuttle, client), to=self._state.replicaList[(self._state.index + 1)])
            super()._label('_st_label_598', block=False)
            replica = result_shuttle = None

            def ExistentialOpExpr_599():
                nonlocal replica, result_shuttle
                for (_, _, (_ConstantPattern618_, replica, result_shuttle)) in self._ReplicaReceivedEvent_3:
                    if (_ConstantPattern618_ == 'result_shuttle'):
                        if True:
                            return True
                return False
            _st_label_598 = 0
            while (_st_label_598 == 0):
                _st_label_598 += 1
                if ExistentialOpExpr_599():
                    _st_label_598 += 1
                else:
                    super()._label('_st_label_598', block=True)
                    _st_label_598 -= 1
    _Replica_handler_430._labels = None
    _Replica_handler_430._notlabels = None

    def _Replica_handler_626(self, replica, forward_shuttle, client):
        self.output('Replica', self._state.index, ':>> Received forward_shuttle from replica')
        action = forward_shuttle.action
        key = action.operation.command[0]
        value = action.operation.command[1]
        self._state.database[key] = value
        result = 'ok'
        if (self._state.type == 'normal_replica'):
            self.output('Replica', self._state.index, ':>> Replica is normal_replica')
            order_proof_list = forward_shuttle.order_proof_list
            result_proof_list = forward_shuttle.result_proof_list
            class_order_proof = da.import_da('OrderProof')
            order_proof = class_order_proof.OrderProof()
            order_proof.order = 'order'
            order_proof.action = action
            order_proof_list.append(order_proof)
            class_result_proof = da.import_da('ResultProof')
            result_proof = class_result_proof.ResultProof()
            result_proof.result_const = 'result'
            result_proof.action = action
            result_proof.result = result
            result_proof_list.append(result_proof)
            self.output('Replica', self._state.index, ':>> Forwarding shuttle to Replica: ', (self._state.index + 1))
            self.send(('forward_shuttle', self._id, forward_shuttle, client), to=self._state.replicaList[(self._state.index + 1)])
            super()._label('_st_label_771', block=False)
            replica = result_shuttle = None

            def ExistentialOpExpr_772():
                nonlocal replica, result_shuttle
                for (_, _, (_ConstantPattern790_, replica, result_shuttle)) in self._ReplicaReceivedEvent_5:
                    if (_ConstantPattern790_ == 'result_shuttle'):
                        if True:
                            return True
                return False
            _st_label_771 = 0
            while (_st_label_771 == 0):
                _st_label_771 += 1
                if ExistentialOpExpr_772():
                    _st_label_771 += 1
                else:
                    super()._label('_st_label_771', block=True)
                    _st_label_771 -= 1
        if (self._state.type == 'tail'):
            self.output('Replica', self._state.index, ':>> Replica is tail')
            self.send(('res_operation', result), to=client)
            self.output('Replica', self._state.index, ':>> Replied res_operation to Client')
            order_proof_list = forward_shuttle.order_proof_list
            result_proof_list = forward_shuttle.result_proof_list
            class_order_proof = da.import_da('OrderProof')
            order_proof = class_order_proof.OrderProof()
            order_proof.order = 'order'
            order_proof.action = action
            order_proof_list.append(order_proof)
            class_result_proof = da.import_da('ResultProof')
            result_proof = class_result_proof.ResultProof()
            result_proof.result_const = 'result'
            result_proof.action = action
            result_proof.result = result
            result_proof_list.append(result_proof)
            class_shuttle = da.import_da('Shuttle')
            result_shuttle = class_shuttle.Shuttle()
            self.output('Replica', self._state.index, ':>> Inserting action in result shuttle', action)
            result_shuttle.action = action
            result_shuttle.order_proof_list = order_proof_list
            result_shuttle.result_proof_list = result_proof_list
            self.send(('result_shuttle', self._id, result_shuttle), to=self._state.replicaList[(self._state.index - 1)])
            super()._label('_st_label_917', block=False)
            replica = client = forward_shuttle = None

            def ExistentialOpExpr_918():
                nonlocal replica, client, forward_shuttle
                for (_, _, (_ConstantPattern936_, replica, forward_shuttle, client)) in self._ReplicaReceivedEvent_6:
                    if (_ConstantPattern936_ == 'forward_shuttle'):
                        if True:
                            return True
                return False
            _st_label_917 = 0
            while (_st_label_917 == 0):
                _st_label_917 += 1
                if ExistentialOpExpr_918():
                    _st_label_917 += 1
                else:
                    super()._label('_st_label_917', block=True)
                    _st_label_917 -= 1
    _Replica_handler_626._labels = None
    _Replica_handler_626._notlabels = None

    def _Replica_handler_945(self, replica, result_shuttle):
        self.output('Replica', self._state.index, ':>> Received result_shuttle from next replica')
        action = result_shuttle.action
        self._state.history[action] = result_shuttle
        self.output('Replica', self._state.index, ':>> Added result shuttle into its history')
        if (self._state.type == 'head'):
            super()._label('_st_label_983', block=False)
            client_index = client = operation = None

            def ExistentialOpExpr_984():
                nonlocal client_index, client, operation
                for (_, _, (_ConstantPattern1005_, operation, client, client_index)) in self._ReplicaReceivedEvent_8:
                    if (_ConstantPattern1005_ == 'req_operation'):
                        if True:
                            return True
                return False
            _st_label_983 = 0
            while (_st_label_983 == 0):
                _st_label_983 += 1
                if ExistentialOpExpr_984():
                    _st_label_983 += 1
                else:
                    super()._label('_st_label_983', block=True)
                    _st_label_983 -= 1
        else:
            self.send(('result_shuttle', self._id, result_shuttle), to=self._state.replicaList[(self._state.index - 1)])
            super()._label('_st_label_1024', block=False)
            forward_shuttle = client = replica = None

            def ExistentialOpExpr_1025():
                nonlocal forward_shuttle, client, replica
                for (_, _, (_ConstantPattern1044_, replica, forward_shuttle, client)) in self._ReplicaReceivedEvent_9:
                    if (_ConstantPattern1044_ == 'forward_shuttle'):
                        if True:
                            return True
                return False
            _st_label_1024 = 0
            while (_st_label_1024 == 0):
                _st_label_1024 += 1
                if ExistentialOpExpr_1025():
                    _st_label_1024 += 1
                else:
                    super()._label('_st_label_1024', block=True)
                    _st_label_1024 -= 1
    _Replica_handler_945._labels = None
    _Replica_handler_945._notlabels = None
