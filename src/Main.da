def main():
    class_olympus = import_da('Olympus')
    class_client = import_da('Client')
    olympus = new(class_olympus.Olympus, args=(), at='OlympusNode')
    clientList = new(class_client.Client, num= 3)
    for client in clientList:
        setup(client, args=(olympus,))

    config(channel = Fifo, clock = Lamport)
    start(olympus)
    start(clientList)
